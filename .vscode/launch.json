{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: server dev",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}\\server"
        }
    ]
}